// productModule.js

export function addProduct(product) {
    console.log('Adding product:', product);
    // Add product to the database or API
}

export function deleteProduct(productId) {
    console.log('Deleting product:', productId);
    // Delete product by productId from the database or API
}

export function updateProduct(productId, updatedData) {
    console.log('Updating product:', productId, updatedData);
    // Update product details by productId
}
